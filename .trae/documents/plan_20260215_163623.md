# Deployment Fix Plan: Import Path Errors (Part 4)

## 1. Analysis
*   **Error**: `Type error: Cannot find module '@/lib/utils'`.
*   **Cause**:
    *   I moved `lib/prisma.ts` to `src/lib/prisma.ts` in a previous step.
    *   However, `lib/utils.ts` (used by `cn` function for Tailwind) might still be in the root `lib` folder or missing entirely if I accidentally deleted it when moving `prisma`.
    *   Alternatively, it might be in `src/lib/utils.ts` but I need to confirm its existence.

## 2. Investigation
*   Check if `src/lib/utils.ts` exists.
*   Check if `lib/utils.ts` exists in root.

## 3. Solution Strategy
*   **If missing**: Create `src/lib/utils.ts` with the standard `cn` helper (clsx + tailwind-merge).
*   **If in root**: Move to `src/lib/utils.ts`.

## 4. Execution Steps
1.  **Check Files**: List files in `src/lib`.
2.  **Create/Move**: Ensure `utils.ts` is in `src/lib`.
3.  **Content**:
    ```typescript
    import { type ClassValue, clsx } from "clsx"
    import { twMerge } from "tailwind-merge"

    export function cn(...inputs: ClassValue[]) {
      return twMerge(clsx(inputs))
    }
    ```

